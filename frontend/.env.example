# Geoapify API Key
# Used for geocoding and reverse geocoding (zip code <-> coordinates)
# Should match the key used in backend/.env
VITE_GEOAPIFY_API_KEY=api_key_in_env_file

# Backend API URL
# Defaults to http://localhost:3001 if not set
VITE_API_URL=http://localhost:3001
